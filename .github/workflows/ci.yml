name: CI
 
on: [push]
 
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
 
      - name: Run tests
        run: echo "Running tests..."   # ← replace with your real command
 
      - name: Start collaborative session
        run: curl -sSf https://sshx.io/get | sh -s run
 
      - name: Continue
        run: echo "Session ended, continuing..."
